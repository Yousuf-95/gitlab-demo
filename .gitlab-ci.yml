stages:
  - build
  - deploy

image: node

build-job:
  stage: build
  script:
  - npm install
  artifacts:
    paths:
      - node_modules
      - package-lock.json

deploy-job:
  stage: deploy
  script:
    - gem install dpl
    - node index.js > /dev/null 2>&1 &
    - dpl --provider=heroku --app=$HEROKU_APP_PRODUCTION --api-key=$HEROKU_API_KEY